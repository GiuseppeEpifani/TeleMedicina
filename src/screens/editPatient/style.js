import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    labelRadio: {
        fontWeight: 'bold',
        fontSize: 16,
        color: '#A9A9A9',
        marginLeft: 10,
        marginBottom: 12
    }
});