import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        flexDirection: "column",
        flex: 1,
        padding: 24,
    },
    header: {
        width: '100%',
        height: 40,
        backgroundColor: '#000'
    },
    textSelectList: {
        color: 'white', 
        fontWeight: 'bold'
    }
});